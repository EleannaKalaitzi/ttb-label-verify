# Fixture label images

Synthetic distilled-spirits labels, one per compliance scenario. Generated by
`npm run fixtures` from [`src/lib/fixtures/specs.ts`](../src/lib/fixtures/specs.ts).
Each ships with its expected verdict — this is both the demo set and the
measurement corpus. Upload any of these in the app (mock mode needs no key).

| # | Image | Targets | Expected |
|---|---|---|---|
| 01-clean-pass | `01-clean-pass.png` | Baseline — all checks pass. Also demonstrates cosmetic brand matching: the label reads STONE'S THROW, the application reads Stone's Throw, and that difference does NOT fail. | **PASS** — Everything matches and complies (cosmetic brand-case difference ignored). |
| 02-warning-title-case | `02-warning-title-case.png` | § 16.22 — the two-word prefix is not in capital letters. | **FAIL** — Prefix not in capital letters → FAIL (§ 16.22). |
| 03-warning-fully-bold | `03-warning-fully-bold.png` | § 16.22 remainder rule — the routinely-missed check. | **FLAG** — Remainder appears bold → FLAG (§ 16.22); bold detection is advisory. |
| 04-warning-missing | `04-warning-missing.png` | § 16.21 — the mandatory warning is absent. | **FAIL** — Warning is mandatory and absent → FAIL (§ 16.21). |
| 05-bourbon-38 | `05-bourbon-38.png` | Standards of identity — non-compliant even though it matches its application perfectly (brand, class, and ABV all agree). | **FAIL** — Below 40% minimum bottling strength → FAIL (§ 5.143), despite matching the application. |
| 06-proof-inconsistent | `06-proof-inconsistent.png` | Internal-consistency cross-check (proof ≈ 2 × ABV), independent of the application. | **FLAG** — Proof 100 implies 50% ABV but the label says 45% → FLAG (internal inconsistency). |
